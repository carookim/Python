# 클래스 생성
class MyData():
    def __init__(self):
        x = ''

# 클래스 객체 생성
m1 = MyData()

# 객체를 사용
# m1.인스턴스변수
#예시)
m1.x = '123'
print(m1.x)

# m1.메소드

# 클래스 내부에 변수 형식을 지정할 수 있다.
# 클래스 내부에 임의의 메소드 기능을 지원하게 정의를 할 수 있다.
# 이 두가지 기능을 지원해주는 게 그 클래스에 포함되면서 얻을수있는 클래스의 기능이다.